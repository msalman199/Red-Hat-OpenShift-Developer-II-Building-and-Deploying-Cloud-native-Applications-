apiVersion: tekton.dev/v1beta1
kind: Pipeline
metadata:
  name: parameterized-build-deploy
  namespace: your-project-name
spec:
  params:
    - name: git-url
      type: string
      description: Git repository URL
      default: "https://github.com/your-username/sample-app.git"
    - name: git-revision
      type: string
      description: Git revision to checkout
      default: "main"
    - name: image-name
      type: string
      description: Name of the container image
      default: "sample-app"
    - name: image-tag
      type: string
      description: Tag for the container image
      default: "latest"
    - name: deployment-name
      type: string
      description: Name of the deployment
      default: "sample-app-deployment"
    - name: registry-url
      type: string
      description: Container registry URL
      default: "image-registry.openshift-image-stream.svc:5000"
  workspaces:
    - name: shared-data
    - name: git-credentials
      optional: true
  tasks:
    - name: fetch-source
      taskRef:
        name: git-clone
        kind: ClusterTask
      workspaces:
        - name: output
          workspace: shared-data
        - name: basic-auth
          workspace: git-credentials
      params:
        - name: url
          value: $(params.git-url)
        - name: revision
          value: $(params.git-revision)
    - name: build-image
      taskRef:
        name: buildah
        kind: ClusterTask
      runAfter:
        - fetch-source
      workspaces:
        - name: source
          workspace: shared-data
      params:
        - name: IMAGE
          value: "$(params.registry-url)/$(context.pipelineRun.namespace)/$(params.image-name):$(params.image-tag)"
        - name: DOCKERFILE
          value: "./Dockerfile"
    - name: deploy-application
      taskRef:
        name: openshift-client
        kind: ClusterTask
      runAfter:
        - build-image
      params:
        - name: SCRIPT
          value: |
            oc set image deployment/$(params.deployment-name) \
              $(params.deployment-name)=$(params.registry-url)/$(context.pipelineRun.namespace)/$(params.image-name):$(params.image-tag)
            oc rollout status deployment/$(params.deployment-name)
