apiVersion: v1
kind: Service
metadata:
  name: multi-env-app-service
  namespace: multi-env-prod
spec:
  selector:
    app: multi-env-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: ClusterIP
